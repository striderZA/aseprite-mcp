[project]
name = "aseprite-mcp"
version = "0.1.0"
description = "MCP server for programmatic control of Aseprite"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
    "python-dotenv>=1.0.0",
]
